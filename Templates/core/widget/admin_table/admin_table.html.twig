<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <table>
        <thead>
            {% for column in columns %}
                <th>{{ column['th'] }}</th>
            {% endfor %}
        </thead>
        <tbody>
            {% for row in rows %}
                {{ row|raw }}
            {% endfor %}
        </tbody>
    </table>
</body>
</html>