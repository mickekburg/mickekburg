<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{{ meta_title }}</title>
    {{ css|raw }}
    {{ js|raw }}
</head>
<body>
    <header class='d-flex'>
        <a class='logo text-center fs24' href='{{ "/" | site_url(true) }}'>CRM</a>
        <div class='top-menu'>
            {{ top1 | raw }}
        </div>
        {{ userInfo | raw}}
    </header>

    <aside class='left-side'>
        <div class="left-side-overlay"></div>
        {{ leftMenu | raw}}
        <a href='{{ "/" | site_url(true) }}' class='hide-sidebar'><i class="fas fa-angle-left"></i></a>
        <div class='copyright'>&copy;2012-{{ 'now' | date('Y') }} <a href='#' target='blank' class='underline color-white ml10'>Документация</a></div>
    </aside>

    <div class='content'>
        {{ content | raw }}
    </div>
</body>
</html>
